{
  "version": 2,
  "buildCommand": "cd my-app && npm install && npm run build",
  "outputDirectory": "my-app/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}